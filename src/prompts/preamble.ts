/**
 * The L0 preamble — the bot's role in one paragraph. Re-exported from
 * `./composer.js` so callers that only want the constant don't pull in
 * the full composer dependency graph (db, tools, fs).
 */
export { PREAMBLE } from './composer.js';
